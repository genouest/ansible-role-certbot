---

- name: Install certbot-plugin-gandi with 'pip'
  ansible.builtin.pip:
    name: "{{ certbot_pip_version }}"
    virtualenv: /srv/venv
    virtualenv_command: "{{ ansible_python_interpreter }} -m venv"
